# ADR-0001：分层 README 结构

## 状态

已接受

## 决策

采用分层 README 结构：

- 根 `README.md` 只做项目地图
- `docs/` 用于产品、架构、流程和决策
- 各目录使用本地 `README.md` 说明边界和常见动作

## 原因

- 控制上下文大小
- 防止根文档无限膨胀
- 让执行动作有据可依
- 支持先计划、后实现、默认 TDD 的工作方式

## 结果

- 改代码前需要先读最近目录的 README
- 详细信息应该从根 README 下沉
- 架构变化需要补 ADR
