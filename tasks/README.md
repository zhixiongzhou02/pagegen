# 任务流

任务文件记录执行状态，不记录架构说明。

## 结构

- `backlog.md`：候选任务
- `in-progress/`：当前执行中的计划
- `done/`：已完成任务的结果归档

## 规则

- 每个激活任务都应该有短计划
- 如果任务中途改范围，要更新任务内容
- 任务完成后保留结果，不保留完整聊天过程
- README 不记录执行中任务细节
- 聊天记录不作为正式任务系统
